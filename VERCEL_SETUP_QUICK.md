# إعداد سريع لـ Vercel - Quick Vercel Setup

## 🚀 خطوات النشر السريع

### 1. إعداد متغيرات البيئة (الأهم!)

#### عبر Vercel Dashboard:

1. اذهب إلى: https://vercel.com/dashboard
2. اختر مشروعك
3. اذهب إلى: **Settings** → **Environment Variables**
4. أضف المتغير التالي:

```
Name: EMAIL_PASSWORD
Value: bdcxkhvhetngnaye
Environments: ✅ Production ✅ Preview ✅ Development
```

5. اضغط **Save**

#### عبر CLI:

```bash
# تثبيت Vercel CLI
npm install -g vercel

# تسجيل الدخول
vercel login

# إضافة المتغير
vercel env add EMAIL_PASSWORD production
# أدخل: bdcxkhvhetngnaye

vercel env add EMAIL_PASSWORD preview
# أدخل: bdcxkhvhetngnaye

# عرض المتغيرات
vercel env ls
```

---

### 2. إعادة النشر

#### من Dashboard:

1. اذهب إلى **Deployments**
2. اضغط على النشر الأخير
3. اضغط على **⋯** (ثلاث نقاط)
4. اختر **Redeploy**
5. انتظر 1-2 دقيقة

#### من CLI:

```bash
vercel --prod
```

#### من Git:

```bash
git add .
git commit -m "Update contact form configuration"
git push
```

---

### 3. التحقق من النجاح

#### أ. تحقق من Logs:

1. اذهب إلى **Deployments** → اختر آخر نشر
2. افتح **Functions**
3. ابحث عن `/api/contact`
4. ابحث في الـ Logs عن:
   - ✅ `Email sent successfully`
   - ❌ `EMAIL_PASSWORD is not configured` (إذا ظهر، المتغير غير مضاف)

#### ب. اختبر النموذج:

1. افتح موقعك: `https://your-domain.vercel.app/contact`
2. املأ النموذج:
   ```
   Name: اختبار Test
   Email: test@example.com
   Message: This is a test message
   ```
3. اضغط **إرسال الرسالة**
4. يجب أن ترى: **"تم إرسال رسالتك بنجاح!"**

#### ج. تحقق من البريد:

- افتح: `Goodwill.laundries@gmail.com`
- تحقق من الوارد (Inbox) والرسائل غير المرغوب فيها (Spam)
- يجب أن تجد رسالة بعنوان: **"رسالة جديدة من موقع طريق الخير - اختبار Test"**

---

## 🔍 استكشاف الأخطاء

### خطأ: "Email service is not configured"

**السبب:** متغير `EMAIL_PASSWORD` غير موجود

**الحل:**

1. تأكد من إضافة المتغير في **Environment Variables**
2. تأكد من اختيار **Production** في Environments
3. أعد النشر

### خطأ: "Email authentication failed"

**السبب:** App Password خاطئ أو منتهي

**الحل:**

1. أنشئ App Password جديد من Gmail
2. حدّث قيمة `EMAIL_PASSWORD`
3. أعد النشر

### الرسائل لا تصل

**تحقق من:**

- ✅ مجلد Spam في البريد
- ✅ البريد المستلم صحيح: `Goodwill.laundries@gmail.com`
- ✅ Vercel Function Logs لا تظهر أخطاء

---

## 📋 قائمة التحقق النهائية

قبل إغلاق هذا الملف، تأكد من:

- [ ] أضفت `EMAIL_PASSWORD` في Vercel Environment Variables
- [ ] اخترت Production + Preview + Development
- [ ] أعدت نشر المشروع
- [ ] اختبرت النموذج على الموقع المباشر
- [ ] وصلت رسالة اختبار إلى البريد
- [ ] رسائل النجاح/الخطأ تظهر بالعربية

---

## 🎯 ملاحظات مهمة

1. **الأمان:**

   - ✅ نستخدم App Password وليس كلمة مرور Gmail الأصلية
   - ✅ المتغيرات آمنة ومشفرة في Vercel
   - ✅ لا تظهر في الكود المصدري

2. **الأداء:**

   - ⚡ إرسال فوري (1-3 ثواني)
   - 📧 تنسيق HTML جميل وعربي
   - ✉️ إمكانية الرد مباشرة على المرسل

3. **الحدود:**
   - Gmail: 500 رسالة/يوم للحسابات العادية
   - Vercel Functions: 100GB-hours مجاناً/شهر (كافي جداً)

---

## 🆘 هل لا يزال لا يعمل؟

إذا استمرت المشكلة بعد اتباع الخطوات:

1. **افتح Console في المتصفح** (F12)
2. **اذهب إلى Network tab**
3. **أرسل رسالة من النموذج**
4. **اضغط على `/api/contact`**
5. **انظر الـ Response**

شارك الـ Response هنا أو في الـ issue للمساعدة.

---

<div align="center">
<h2>✅ بعد اتباع هذه الخطوات، النموذج سيعمل 100%!</h2>
</div>

<div align="center" dir="rtl">
<h3>🎉 بالتوفيق! 🎉</h3>
</div>
