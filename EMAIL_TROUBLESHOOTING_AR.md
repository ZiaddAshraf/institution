# مشكلة إرسال البريد الإلكتروني - الحل

## ⚠️ المشكلة
يظهر خطأ "حدث خطأ أثناء إرسال الرسالة" عند إرسال النموذج.

## 🔍 السبب المحتمل
كلمة المرور "tariq" التي تم إدخالها قصيرة جداً. كلمات مرور تطبيقات Gmail تكون **16 حرفاً**.

## ✅ الحل

### الخطوة 1: إنشاء كلمة مرور تطبيق Gmail الصحيحة

1. اذهب إلى: https://myaccount.google.com/apppasswords
2. سجل الدخول بحساب: **ziaddd155@gmail.com**
3. إذا لم تفعّل التحقق بخطوتين:
   - اذهب إلى: https://myaccount.google.com/security
   - فعّل "التحقق بخطوتين" أولاً
   - ثم ارجع لإنشاء كلمة مرور التطبيق

4. في صفحة كلمات مرور التطبيقات:
   - اختر "البريد" من القائمة المنسدلة
   - اختر "جهاز آخر (اسم مخصص)"
   - اكتب: "Goodwill Website"
   - اضغط "إنشاء"

5. ستظهر لك كلمة مرور من 16 حرفاً مثل:
   ```
   abcd efgh ijkl mnop
   ```

### الخطوة 2: تحديث ملف .env.local

1. افتح ملف `.env.local` في مشروعك
2. استبدل السطر الحالي:
   ```
   EMAIL_PASSWORD=tariq
   ```
   بـ:
   ```
   EMAIL_PASSWORD=abcdefghijklmnop
   ```
   (بدون مسافات - 16 حرف متصل)

### الخطوة 3: إعادة تشغيل الخادم

**مهم جداً:** يجب إعادة تشغيل خادم التطوير بعد تعديل `.env.local`

1. في Terminal، اضغط `Ctrl+C` لإيقاف الخادم
2. شغّل الخادم من جديد:
   ```bash
   npm run dev
   ```

### الخطوة 4: اختبار النموذج

1. افتح الموقع على المتصفح
2. اذهب لصفحة "اتصل بنا"
3. املأ النموذج وأرسله
4. تحقق من بريدك الإلكتروني على ziaddd155@gmail.com

## 🧪 اختبار التكوين

يمكنك زيارة هذا الرابط للتحقق من التكوين:
```
http://localhost:3001/api/test-email
```

يجب أن يظهر:
```json
{
  "configured": true,
  "passwordLength": 16,
  "message": "Email password is configured (16 characters)"
}
```

## 📝 ملاحظات مهمة

- كلمة مرور التطبيق تختلف عن كلمة مرور Gmail العادية
- يجب أن تكون 16 حرفاً بدون مسافات
- لا تشارك كلمة المرور مع أحد
- يمكنك إلغاء كلمة المرور في أي وقت وإنشاء واحدة جديدة

## 🆘 إذا استمرت المشكلة

تحقق من:
1. أن التحقق بخطوتين مفعّل على حساب Gmail
2. أن كلمة المرور 16 حرفاً بالضبط
3. أنك قمت بإعادة تشغيل الخادم بعد التعديل
4. راجع رسائل الخطأ في Terminal

## 🔗 روابط مفيدة

- إنشاء كلمة مرور تطبيق: https://myaccount.google.com/apppasswords
- تفعيل التحقق بخطوتين: https://myaccount.google.com/security
- دعم Google: https://support.google.com/accounts/answer/185833
